import type { Theme } from '@/types/contexts/ThemeContextType';

export const BASE_API_URL: string = 'https://test-front.framework.team';
export const DEFAULT_THEME: Theme = 'dark';
export const PAGINATE_LIMIT: number = 6;
